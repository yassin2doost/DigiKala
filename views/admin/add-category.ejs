<% layout('layout') -%>

<head>
    <title>
        دسته ها
    </title>
</head>

<div class="container bootstrap snippet">
  <div class="row">
    <div class="col-lg-4 col-md-offset-3">
        <% if(message.length > 0) { %>
            <div class="alert alert-success alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                <%= message %>
            </div>
            <% } %>
            <form method="post">
        <h1 class="list-group-item text-muted"> افزودن دسته جدید</h1>
        <label="name">نام دسته جدید:</label>
        <input type="name" class="form-control" name="name">
        <button type="submit" class="btn btn-info"> افزودن دسته جدید </button>
             </form>
         </div>
    </div>
</div>